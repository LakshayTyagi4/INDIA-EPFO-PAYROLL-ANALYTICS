# Power Query

M code for the PDF-extraction and folder-combine pipeline that turns the raw
monthly PDFs in `data/raw/` into one unified table. Paste into Power BI's
Advanced Editor to inspect or reuse.
