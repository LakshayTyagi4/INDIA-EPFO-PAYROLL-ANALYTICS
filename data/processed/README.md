# Processed data

`payroll_monthly.csv` — the cleaned, combined output of the Power Query extraction
(one row per month per age band). Exported here for transparency, so the dataset
is inspectable on GitHub without opening Power BI Desktop.

Regenerate by refreshing the .pbix and re-exporting the underlying table.
